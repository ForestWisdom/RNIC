# Project Instructions

- Use the remote server `4090-2` with `~/zhangzhisen` as the server-side workspace for this project.
- Make code changes locally first. After local changes are ready, upload them to GitHub, then pull the updated branch on `4090-2` and run tests from `~/zhangzhisen`.
